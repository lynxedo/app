// Shared admin toolkit (audit AD-toolkit). Import from '@/components/admin'.
export { AdminCard } from './AdminCard'
// The accessible on/off switch is the same one the rest of the app uses.
export { Toggle as ToggleSwitch } from '@/components/ui'
export { useAutoSave } from '@/hooks/use-auto-save'
